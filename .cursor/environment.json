{
  "name": "RDO Venko",
  "terminals": [
    {
      "name": "static-server",
      "command": "cd \"$(git rev-parse --show-toplevel)\" && python3 -m http.server 8080",
      "description": "Serves the RDO Venko static site (index.html, bootstrap.html, config.js) on http://localhost:8080. Pure HTML/JS — no build step. Data lives in a hosted Supabase backend configured in config.js. Restart this terminal if the server is not running."
    }
  ],
  "ports": [
    {
      "name": "web",
      "port": 8080
    }
  ]
}
