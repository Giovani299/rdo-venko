{
  "name": "RDO Venko",
  "terminals": [
    {
      "name": "static-server",
      "command": "cd \"$(git rev-parse --show-toplevel)\" && python3 -m http.server 8080",
      "description": "Serves the RDO Venko static site (index.html, bootstrap.html, config.js) on port 8080. Pure HTML/JS — no build step. Data lives in a hosted Supabase backend configured in config.js."
    }
  ],
  "ports": [
    {
      "name": "web",
      "port": 8080
    }
  ]
}
